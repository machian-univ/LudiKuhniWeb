html(lang="ru")
head
  meta(charset='UTF-8')
  meta(name='viewport' content='width=device-width, initial-scale=1.0')
  link(href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous")
  link(rel='stylesheet' href='../css/slider_css.css')
  //- link(rel='stylesheet' href='../js/validation_client.js')
  title Мебельный салон Люди Кухни
body.bck
  include header.pug
  .container
    .row
      .col-8.mx-auto.my-auto.form-bck
        .card-body.p-lg-5
          h1.m-4.fw-light.first-text.fw-bold.text-uppercase Регистрация
          .pt-lg-3
            form(name="reg" novalidate='novalidate' action='/signup' accept-charset='UTF-8' method='POST')
              if (message.length > 0)
                div.alert.alert-danger #{message}
              if (errors.length > 0)
                for error in errors
                for error in errors
                  div.alert.alert-danger #{error.msg}
              .mb-2.email.required.user_sign_up_form_email
                  label.form-label.email.required(for='user_email')
                      | Электронная почта
                  p.px-2.text-muted Пароль должен состоять из цифр и латиских букв и содержать минимум 6 символов
                  input#user_email.form-control.string.email.border(autofocus='autofocus' type='email' name='username' required)
              .mb-2.string.required.user_sign_up_form_first_name
                  label.form-label.string.required(for='user_sign_up_form_first_name') Имя
                  input#user_sign_up_form_first_name.form-control.string.border(type='text' name='name' required)
              .mb-2.password.required.user_sign_up_form_password
                  label.form-label.password.required(for='user_sign_up_form_password') Пароль
                  input#user_sign_up_form_password.form-control.password.border(type='password' name='password' pattern="[A-Za-z] [0-9]"  minlength="6" maxlength="20" required)
              input.btn.btn-primary.w-100(type='submit' name='commit' value='Зарегистрироваться' data-disable-with='Зарегистрироваться')
              script(src="../js/validation_client.js")
  include footer.pug
